services:
  sqlpublisher:
    image: mcr.microsoft.com/mssql/server:2019-latest
    container_name: sqlpublisher
    hostname: sqlpublisher
    environment:
      - ACCEPT_EULA=Y
      - SA_PASSWORD=YourStrong@Passw0rd
      - MSSQL_AGENT_ENABLED=true
    ports:
      - "1433:1433"
    networks:
      - sql_repl_network
    volumes:
      - publisher_data:/var/opt/mssql
      - ./publisher-setup-push.sql:/var/opt/mssql/publisher-setup-push.sql:ro
      - ./publisher-setup-pull.sql:/var/opt/mssql/publisher-setup-pull.sql:ro
    healthcheck:
      test: /opt/mssql-tools18/bin/sqlcmd -S localhost -U sa -P "YourStrong@Passw0rd" -Q "SELECT 1" -C || exit 1
      interval: 10s
      timeout: 3s
      retries: 10
      start_period: 10s

  sqlsubscriber:
    image: mcr.microsoft.com/mssql/server:2019-latest
    container_name: sqlsubscriber
    hostname: sqlsubscriber
    environment:
      - ACCEPT_EULA=Y
      - SA_PASSWORD=YourStrong@Passw0rd
      - MSSQL_AGENT_ENABLED=true
    ports:
      - "1434:1433"
    networks:
      - sql_repl_network
    volumes:
      - subscriber_data:/var/opt/mssql
      - ./subscriber-setup-push.sql:/var/opt/mssql/subscriber-setup-push.sql:ro
      - ./subscriber-setup-pull.sql:/var/opt/mssql/subscriber-setup-pull.sql:ro
    healthcheck:
      test: /opt/mssql-tools18/bin/sqlcmd -S localhost -U sa -P "YourStrong@Passw0rd" -Q "SELECT 1" -C || exit 1
      interval: 10s
      timeout: 3s
      retries: 10
      start_period: 10s

networks:
  sql_repl_network:
    driver: bridge

volumes:
  publisher_data:
  subscriber_data:
